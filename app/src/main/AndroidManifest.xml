<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.store.inventory">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-feature android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.store.inventory.activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.store.inventory.activities.DetailsActivity" />
        <activity android:name="com.store.inventory.activities.EditActivity" />
        <activity android:name="com.store.inventory.activities.VerifyPhoneNumberActivity" />
        <activity android:name="com.store.inventory.activities.SetProfileData" />
        <activity android:name="com.store.inventory.activities.RegisterPhoneNumber" />
        <activity android:name=".activities.MyInventoryActivity" />
        <activity android:name=".activities.MyDashboardActivity" />
        <activity android:name=".activities.ContactUs" />

    </application>

</manifest>